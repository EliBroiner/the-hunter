<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="@drawable/widget_background"
    android:layoutDirection="rtl"
    android:textDirection="rtl">

    <!-- Search Bar - Sticky Header, תמיד גלוי -->
    <LinearLayout
        android:id="@+id/search_bar"
        android:layout_width="match_parent"
        android:layout_height="44dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:background="@drawable/search_bar_background"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:layoutDirection="rtl">

        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@android:drawable/ic_menu_search"
            android:tint="#6366F1" />

        <TextView
            android:id="@+id/search_hint"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="10dp"
            android:text="לחץ לחיפוש..."
            android:textColor="#888888"
            android:textSize="13sp"
            android:textDirection="rtl" />
    </LinearLayout>

    <!-- רשימה גלילה — RemoteViewsService -->
    <FrameLayout
        android:id="@+id/files_list_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="10dp"
        android:minHeight="120dp"
        android:visibility="gone">

        <ListView
            android:id="@+id/widget_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@android:color/transparent"
            android:dividerHeight="8dp"
            android:scrollbars="none"
            android:layoutDirection="rtl" />

        <!-- גרדיאנט בתחתית — מרמז שיש עוד תוכן לגלול -->
        <View
            android:layout_width="match_parent"
            android:layout_height="24dp"
            android:layout_gravity="bottom"
            android:background="@drawable/widget_bottom_fade" />
    </FrameLayout>

    <!-- כפתור כשהמטמון ריק -->
    <LinearLayout
        android:id="@+id/start_hunting_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:background="@drawable/search_bar_background"
        android:padding="14dp"
        android:gravity="center"
        android:visibility="visible"
        android:layoutDirection="rtl">

        <TextView
            android:id="@+id/empty_state_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/widget_start_hunting"
            android:textColor="#888888"
            android:textSize="13sp"
            android:textDirection="rtl" />
    </LinearLayout>
</LinearLayout>
