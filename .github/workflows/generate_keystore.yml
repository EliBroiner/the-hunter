name: Generate Keystore & SHA-1

on:
  workflow_dispatch:  # הרצה ידנית בלבד

jobs:
  generate-keystore:
    runs-on: ubuntu-latest
    
    steps:
      - name: Generate Upload Keystore
        run: |
          echo "🔐 Generating upload-keystore.jks..."
          keytool -genkey -v \
            -keystore upload-keystore.jks \
            -alias upload \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -storepass MySecurePassword123 \
            -keypass MySecurePassword123 \
            -dname "CN=The Hunter, OU=Development, O=TheHunter, L=Tel Aviv, ST=Israel, C=IL"
          
          echo "✅ Keystore generated successfully!"
          ls -la upload-keystore.jks

      - name: Print SHA-1 Fingerprint
        run: |
          echo ""
          echo "══════════════════════════════════════════════════════════════"
          echo "📋 SHA-1 FINGERPRINT (Copy this to Firebase Console):"
          echo "══════════════════════════════════════════════════════════════"
          keytool -list -v \
            -keystore upload-keystore.jks \
            -alias upload \
            -storepass MySecurePassword123 | grep -A1 "SHA1:"
          echo "══════════════════════════════════════════════════════════════"
          echo ""

      - name: Print SHA-256 Fingerprint
        run: |
          echo ""
          echo "══════════════════════════════════════════════════════════════"
          echo "📋 SHA-256 FINGERPRINT (Also useful for Firebase):"
          echo "══════════════════════════════════════════════════════════════"
          keytool -list -v \
            -keystore upload-keystore.jks \
            -alias upload \
            -storepass MySecurePassword123 | grep -A1 "SHA256:"
          echo "══════════════════════════════════════════════════════════════"
          echo ""

      - name: Print Full Certificate Info
        run: |
          echo ""
          echo "══════════════════════════════════════════════════════════════"
          echo "📋 FULL CERTIFICATE INFO:"
          echo "══════════════════════════════════════════════════════════════"
          keytool -list -v \
            -keystore upload-keystore.jks \
            -alias upload \
            -storepass MySecurePassword123
          echo "══════════════════════════════════════════════════════════════"
          echo ""

      - name: Print Base64 Encoded Keystore
        run: |
          echo ""
          echo "══════════════════════════════════════════════════════════════"
          echo "📋 BASE64 KEYSTORE (Copy this to GitHub Secrets as KEYSTORE_BASE64):"
          echo "══════════════════════════════════════════════════════════════"
          echo ""
          base64 -w 0 upload-keystore.jks
          echo ""
          echo ""
          echo "══════════════════════════════════════════════════════════════"
          echo ""

      - name: Print key.properties content
        run: |
          echo ""
          echo "══════════════════════════════════════════════════════════════"
          echo "📋 KEY.PROPERTIES CONTENT (For local development):"
          echo "══════════════════════════════════════════════════════════════"
          echo ""
          echo "storePassword=MySecurePassword123"
          echo "keyPassword=MySecurePassword123"
          echo "keyAlias=upload"
          echo "storeFile=upload-keystore.jks"
          echo ""
          echo "══════════════════════════════════════════════════════════════"

      - name: Summary
        run: |
          echo ""
          echo "╔══════════════════════════════════════════════════════════════╗"
          echo "║                    🎉 GENERATION COMPLETE!                    ║"
          echo "╠══════════════════════════════════════════════════════════════╣"
          echo "║ Next Steps:                                                  ║"
          echo "║ 1. Copy SHA-1 to Firebase Console → Project Settings         ║"
          echo "║    → Your Apps → Android → Add fingerprint                   ║"
          echo "║                                                              ║"
          echo "║ 2. Copy Base64 string to GitHub Secrets:                     ║"
          echo "║    - KEYSTORE_BASE64 = (the base64 string)                   ║"
          echo "║    - KEYSTORE_PASSWORD = MySecurePassword123                 ║"
          echo "║    - KEY_ALIAS = upload                                      ║"
          echo "║    - KEY_PASSWORD = MySecurePassword123                      ║"
          echo "║                                                              ║"
          echo "║ 3. Download new google-services.json from Firebase           ║"
          echo "║    and replace android/app/google-services.json              ║"
          echo "║                                                              ║"
          echo "║ 4. Delete this workflow file after use!                      ║"
          echo "╚══════════════════════════════════════════════════════════════╝"
